# 万能资源圈・错误兜底规则
# 注意：页面部署在「万能资源圈」子目录下，_redirects 需位于 GitHub 仓库根（构建根）才生效
# 规则从上到下匹配，先匹配者先生效

# 1) 站点根：301 重定向到导航页（必须用 301/302，不能用 200，否则 URL 停留在根级，
#    页面内的相对图片路径 assets/images/... 会解析到根目录而全部加载失败）
/ /万能资源圈/index 301

# 2) 其它所有未匹配路径（错误页、不存在的页面、乱输入地址）→ 统一错误页（404）
/* /万能资源圈/error.html 404
