# 万能资源圈・错误兜底规则
# 说明：页面与 API 在同一层级（部署后位于站点根，地址如 /index、/shop、/api/...），
# 因此这里的目标路径都用根级写法（不带「万能资源圈/」前缀）。
# _redirects 需位于 GitHub 仓库根（构建根）才生效；规则从上到下匹配，先匹配者先生效。

# 1) 站点根：301 重定向到导航页（用重定向而非 rewrite，避免 URL 停留在根级导致相对图片路径错乱）
/ /index 301

# 2) 其它所有未匹配路径（错误页、不存在的页面、乱输入地址）→ 统一错误页（404）
/* /error.html 404
