# 万能资源圈・根跳转规则
# 说明：页面与 API 部署在站点根目录（地址如 /index、/shop、/api/...）。
# 注意：此处【不要】写 /* 全局重写规则——它会劫持所有正常页面（/index、/shop 等都会被重写），
#       导致全站变成错误页。404 兜底统一由 functions/[[path]].js 处理（静态资源优先，安全）。
# _redirects 需位于构建根（万能资源圈文件夹）才生效。

# 站点根：301 重定向到导航页（用重定向而非 rewrite，避免 URL 停留在根级导致相对图片路径错乱）
/ /index 301
